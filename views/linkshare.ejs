<!DOCTYPE html>
<html>
<head>
    <link rel="stylesheet" type="text/css" href="teacher.css">
    
</head>
<body>
<div>
  <!-- <form action="/action_page.php"> -->
    <label for="tlnk">Student Session's link:/session/<%=data._id%></label>
    <input type="text" id="myInput" value="localhost:1690/session/<%=data._id%>" hidden>

        <button onclick="myFunction()">/session/<%=data._id%></button>
        <br>
        <br>
    <a href="/session/<%=data._id%>/teacher">Start Session</a>
</div>
<script>
    function myFunction() {
      var copyText = document.getElementById("myInput");
      copyText.select();
      copyText.setSelectionRange(0, 99999)
      document.execCommand("copy");
      alert("Copied the text: " + copyText.value);
    }
    </script>
    
</body>
</html>